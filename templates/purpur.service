[Unit]
Description=PurPur Server
After=network.target

[Service]
Type=simple
User=root
Restart=on-failure
RestartSec=10
WorkingDirectory=/root/mozart409-pupur/server
ExecStart=/usr/bin/java --add-modules=jdk.incubator.vector -Xms1G -Xmx2G -jar paper.jar --nogui

[Install]
WantedBy=multi-user.target